# Filter-Design-for-LiDAR

The aim of this project is to create a Range filter with dynamic scan length, and Temporal Median filter with dynamic window size, to reduce the noise in scans from a LiDAR.
